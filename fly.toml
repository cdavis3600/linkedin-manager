app = "linkedin-manager-cjd"
primary_region = "ewr"

[build]

[env]
  LOG_LEVEL = "INFO"

[mounts]
  source = "linkedin_data"
  destination = "/app/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  auto_stop_machines = "off"
  auto_start_machines = true
